apiVersion: v1
kind: Pod
metadata:
  name: my-pod
  labels:
    app: my-pod
spec:
  containers:
  - name: container1
    image: andymesser/easy-hw-project:v0.1
    ports:
    - containerPort: 8000
      protocol: TCP
      name: http